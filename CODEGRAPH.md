# Yapplr

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-09 12:53 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

Yapplr is a full-stack social media platform repository containing multiple coordinated projects. The backend is an ASP.NET Core REST API (Yapplr.Api) that handles authentication, posts, comments, messaging, following, content moderation, notifications (SignalR, Firebase, Expo push), payments (Stripe/PayPal), analytics (InfluxDB), and administrative functions. It uses Entity Framework Core for persistence, Redis for caching, and MassTransit for async command/event messaging. A dedicated background worker service (Yapplr.VideoProcessor) consumes video upload messages from the MassTransit queue, transcodes videos via HandBrake, generates thumbnails via FFmpeg, and extracts metadata — acting as an asynchronous media processing pipeline. A shared .NET library (Yapplr.Shared) provides common utilities (e.g., environment/Git branch detection) reusable across backend projects. On the client side, a React/TypeScript web frontend (yapplr-frontend) and a React Native mobile app (YapplrMobile) both consume the backend API, sharing type definitions and API client classes through a common JavaScript/TypeScript library (yapplr-shared). The test suite (Yapplr.Api.Tests) uses xUnit, Moq, and an in-memory database to cover payments, notifications, posts, file uploads, moderation, subscriptions, and video processing logic. The repository is self-contained with no detected cross-repository dependencies; the backend API and video processor are the core services consumed by the web and mobile frontends.

## Projects

### Yapplr.VideoProcessor

Yapplr.VideoProcessor is a background worker service that processes video upload requests consumed from a message queue (MassTransit). It transcodes videos using HandBrake, generates thumbnails using FFmpeg, extracts video metadata, and stores processed outputs — acting as an asynchronous media processing pipeline within the Yapplr platform.

### Yapplr.Shared

Yapplr.Shared is a shared library project within the Yapplr repository that provides common utilities and extension methods reusable across other projects in the solution. Based on the available code, it includes environment-related helpers for detecting and configuring the runtime environment, particularly around Git branch detection and local development identification.

### Yapplr.Api

Yapplr.Api is a feature-rich ASP.NET Core social media backend API that handles user authentication, post/comment creation, following, messaging, notifications (SignalR, Firebase, Expo), content moderation, analytics, payment processing (Stripe/PayPal), and administrative functions. It uses Entity Framework Core for persistence, a CQRS pattern via MassTransit for async command processing, Redis for caching, and InfluxDB as an optional external analytics store. The project serves as the central backend service for the Yapplr platform, exposing REST endpoints and a SignalR hub consumed by client applications.

### Yapplr.Api.Tests

Yapplr.Api.Tests is the automated test suite for the Yapplr.Api backend project. It contains unit tests, integration tests, and performance tests covering services for payments, notifications, post management, file uploads, user trust/moderation, subscriptions, video processing, and various utility components. The project uses xUnit as the test framework, Moq for mocking dependencies, and an in-memory database (via TestYapplrDbContext) to exercise data access logic without a real database.

### Yapplr

Yapplr is a social media platform with three client-facing sub-projects and a shared library: a web frontend (yapplr-frontend), a React Native mobile app (YapplrMobile), a shared API/types library (yapplr-shared), and a .NET backend API (Yapplr.Api). The system supports posts, comments, messaging, notifications, user profiles, moderation, subscriptions, analytics, and real-time communication via SignalR. The shared library provides common type definitions and API client classes reused by both the web and mobile frontends.

