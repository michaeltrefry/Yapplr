# Yapplr

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-08 12:25 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

Yapplr is a full-stack social media platform repository comprising multiple projects that form a cohesive system. The backend is built on ASP.NET Core (Yapplr.Api) and exposes a REST API plus SignalR real-time hub supporting user accounts, posts (with media, reactions, comments, hashtags, reposts, and link previews), direct messaging, notifications (SignalR, Firebase, Expo push), content moderation (AI-assisted, trust-score-based, and admin-managed), analytics (with optional InfluxDB), subscription and payment processing (Stripe and PayPal), and administrative tooling with audit logging and appeals. It uses Entity Framework Core for persistence, Redis for caching, and MassTransit for asynchronous command/event processing following a CQRS pattern. A dedicated background microservice (Yapplr.VideoProcessor) consumes video processing requests from the MassTransit message queue, orchestrates transcoding via HandBrake and FFmpeg, generates thumbnails, and returns processed video output, decoupling heavy media workloads from the main API. A shared .NET utility library (Yapplr.Shared) provides common helpers including environment and Git-branch-aware configuration used across backend services. On the client side, a TypeScript shared library (yapplr-shared) defines common type definitions and API client classes consumed by both a web frontend (yapplr-frontend) and a React Native mobile app (YapplrMobile). Automated tests (Yapplr.Api.Tests) cover the API's major subsystems using xUnit, Moq, and an in-memory EF Core database. Overall, the repository implements a vertically integrated social media platform with clear separation between API, async media processing, shared utilities, and multi-platform clients.

## Projects

### Yapplr.VideoProcessor

Yapplr.VideoProcessor is a background worker service responsible for processing video uploads. It consumes VideoProcessingRequest messages from a message queue (via MassTransit), orchestrates video transcoding using HandBrake and FFmpeg, generates thumbnails, and produces processed video output. It operates as an independent microservice within the Yapplr repository, decoupling video processing workloads from the main application.

### Yapplr.Shared

Yapplr.Shared is a shared utility library within the Yapplr repository, providing common functionality reusable across multiple projects or services. Based on the available code, it includes environment and configuration helpers, particularly around detecting and configuring runtime environments based on Git branch context.

### Yapplr.Api

Yapplr.Api is the core ASP.NET Core web API backend for a social media platform called Yapplr. It provides a comprehensive REST API and SignalR real-time hub supporting user accounts, posts (with media, reposts, reactions, comments, hashtags, and link previews), direct messaging, notifications (via SignalR, Firebase, and Expo push), content moderation (AI-assisted, trust-score-based, and manual admin tools), analytics (with optional InfluxDB integration), subscription/payment processing (Stripe and PayPal), and administrative tooling including audit logging and appeals. The project uses a CQRS pattern with MassTransit for asynchronous command processing, Entity Framework Core for persistence, and Redis for caching.

### Yapplr.Api.Tests

Yapplr.Api.Tests is the automated test suite for the Yapplr API, providing unit, integration, and performance tests across all major subsystems including payment processing, notifications, post filtering/visibility, media uploads, video processing, subscriptions, user trust/moderation, and authentication. The project uses xUnit as its test framework, Moq for mocking dependencies, and an in-memory EF Core database (TestYapplrDbContext) to exercise service logic against realistic data scenarios without requiring a live database.

### Yapplr

Yapplr is a social media platform repository consisting of three client-facing projects (a web frontend, a React Native mobile app, and a shared library) plus a .NET backend API. The shared library (yapplr-shared) provides common type definitions and API client classes consumed by both the web frontend (yapplr-frontend) and mobile app (YapplrMobile). The backend (Yapplr.Api) implements a CQRS-based architecture with services for notifications, moderation, analytics, email delivery, and real-time messaging via SignalR.

